<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ezen.reply.ReplyService">

<!-- 댓글 입력 -->
<insert id="replyInput">
	insert into reply values(#{param1}, #{param2}, rep_no.nextval, #{param3}, sysdate, #{param4})
</insert>

<select id="boardreplyout" resultType="com.ezen.reply.ReplyDTO">
	select * from reply where rep_table=#{param1} and rep_originno=#{param2}
</select>

<delete id="boardreplydelete">
	delete from reply where rep_no=#{param1}
</delete>

<select id="replyCount" resultType="int">
	select count(*) from reply where rep_originno=#{rep_originno}
</select>

</mapper>